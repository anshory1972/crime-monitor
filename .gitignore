# Service account key — never commit
keys/

# Large shapefiles (download from HDX separately)
map/

# Raw / large data files (regenerated by pipeline)
data/*.parquet
data/*.csv

# Python cache
__pycache__/
*.pyc
.env

# OS
.DS_Store
Thumbs.db
